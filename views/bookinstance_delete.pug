extends layout 

block content 
    h1= title
    h2= book_instance._id
    p You are about to delete a copy of #[strong #{book_instance.book.title}]

    p Are you sure?
    form(action="" method="post") 
        input(type="hidden", name="bookinstanceid" value=book_instance._id)
        button.btn.btn-primary(type='submit') Delete